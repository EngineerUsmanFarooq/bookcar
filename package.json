{
    "name": "car-booking-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd ../server && npm install",
        "install-all": "npm install && cd frontend && npm install && cd ../server && npm install"
    }
}